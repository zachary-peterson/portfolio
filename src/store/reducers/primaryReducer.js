const initialState = {
    isLoading: false,
    visited: false,
    projects: []
}

export const primaryReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}